import React from 'react'
import { AddChannel } from "../assets";

const TeamChannelPreview = ({ setToggleContainer, children, error = false, loading, type, isCreating, setIsCreating, setCreateType, setIsEditing }) => {
  return (
    <div>TeamChannelPreview</div>
  )
}

export default TeamChannelPreview